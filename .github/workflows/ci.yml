name: CI
on:
  push:
    branches: [main]
  pull_request:
jobs:
  mcp-server:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: agent-tools/mcp-server
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: agent-tools/mcp-server/package-lock.json
      - run: npm ci
      - run: npm run build --if-present
      - run: npm test
